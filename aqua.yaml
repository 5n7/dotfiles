---
# Global aqua config (AQUA_GLOBAL_CONFIG). For CLIs that are awkward or
# unavailable via nix/homebrew. Per-repo aqua.yaml still applies on top.
registries:
  - type: standard
    ref: v4.469.0 # renovate: depName=aquaproj/aqua-registry
packages: []
